{
  "name": "save-serve-complete",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd owner/owner && npm run dev\" \"cd student/student && npm run dev\" \"cd server && npm run dev\" \"cd admin && npm run dev\"",
    "install-all": "npm install && cd owner/owner && npm install && cd ../../student/student && npm install && cd ../../server && npm install && cd ../admin && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@yudiel/react-qr-scanner": "^2.2.1",
    "bcryptjs": "^3.0.2",
    "clsx": "^2.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "framer-motion": "^12.7.4",
    "geist": "^1.3.1",
    "lucide-react": "^0.501.0",
    "mongoose": "^8.13.2",
    "next": "^15.3.1",
    "tailwind-merge": "^3.2.0"
  }
}
